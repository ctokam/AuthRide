<?xml version="1.0" encoding="utf-8"?>
<!-- Οθόνη Επιβάτη: λίστα διαθέσιμων διαδρομών + empty state όταν δεν υπάρχουν. -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:clipChildren="false"
    android:clipToPadding="false"
    tools:context=".PassengerActivity">

    <TextView
        android:id="@+id/tv_passenger_title"
        style="@style/ScreenTitle"
        android:layout_marginTop="@dimen/space_l"
        android:text="@string/passenger_title"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewRoutes"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="@dimen/space_m"
        android:clipToPadding="false"
        android:padding="@dimen/space_m"
        app:layout_constraintTop_toBottomOf="@id/tv_passenger_title"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation" />

    <!-- Spinner όσο φορτώνουν οι διαθέσιμες διαδρομές -->
    <ProgressBar
        android:id="@+id/progress_routes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/tv_passenger_title"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Empty state: εμφανίζεται όταν η λίστα είναι άδεια (visibility VISIBLE μέσω κώδικα) -->
    <LinearLayout
        android:id="@+id/layout_empty_state"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="@dimen/screen_padding"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/tv_passenger_title"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation">

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardCornerRadius="@dimen/card_corner"
            app:cardElevation="@dimen/card_elevation"
            app:cardBackgroundColor="@color/navy">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="vertical"
                android:padding="32dp">

                <ImageView
                    android:layout_width="120dp"
                    android:layout_height="120dp"
                    android:layout_marginBottom="@dimen/space_m"
                    android:contentDescription="@string/no_ride_desc"
                    android:src="@drawable/no_ride" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="@dimen/space_m"
                    android:gravity="center"
                    android:text="@string/empty_routes_title"
                    android:textColor="@color/text_on_primary"
                    android:textSize="18sp"
                    android:textStyle="bold" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="@dimen/space_l"
                    android:gravity="center"
                    android:text="@string/empty_routes_desc"
                    android:textColor="@color/divider"
                    android:textSize="14sp" />

                <Button
                    android:id="@+id/btn_refresh"
                    style="@style/PrimaryButton"
                    android:layout_height="50dp"
                    android:text="@string/btn_refresh" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>
    </LinearLayout>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface"
        android:elevation="12dp"
        app:itemIconTint="@color/bottom_nav_color"
        app:itemTextColor="@color/bottom_nav_color"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/bottom_nav_passenger" />
</androidx.constraintlayout.widget.ConstraintLayout>