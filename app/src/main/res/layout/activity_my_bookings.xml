<?xml version="1.0" encoding="utf-8"?>
<!-- Οθόνη "Οι Κρατήσεις μου" (επιβάτης). Καθρέφτης του activity_my_rides:
     ίδια δομή/tabs/item_route, αλλά τίτλος & bottom nav του επιβάτη. -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:clipChildren="false"
    android:clipToPadding="false"
    tools:context=".MyBookingsActivity">

    <TextView
        android:id="@+id/tv_my_bookings_title"
        style="@style/ScreenTitle"
        android:layout_marginTop="@dimen/space_l"
        android:text="@string/my_bookings_title"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tab_layout_bookings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_m"
        android:background="@color/background"
        app:tabIndicatorColor="@color/green"
        app:tabSelectedTextColor="@color/text_primary"
        app:tabTextColor="@color/text_secondary"
        app:layout_constraintTop_toBottomOf="@id/tv_my_bookings_title">

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/tab_upcoming" />

        <com.google.android.material.tabs.TabItem
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/tab_history" />
    </com.google.android.material.tabs.TabLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewMyBookings"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:padding="@dimen/space_m"
        app:layout_constraintTop_toBottomOf="@id/tab_layout_bookings"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation" />

    <!-- Spinner όσο φορτώνουν τα δεδομένα από το Firestore -->
    <ProgressBar
        android:id="@+id/progress_my_bookings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/tab_layout_bookings"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Empty state όταν δεν υπάρχουν κρατήσεις στο επιλεγμένο tab -->
    <LinearLayout
        android:id="@+id/layout_empty_my_bookings"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="@dimen/screen_padding"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/tab_layout_bookings"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation">

        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginBottom="@dimen/space_m"
            android:alpha="0.6"
            android:contentDescription="@string/no_ride_desc"
            android:src="@drawable/no_ride" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@string/empty_my_bookings"
            android:textColor="@color/text_secondary"
            android:textSize="16sp" />
    </LinearLayout>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface"
        android:elevation="12dp"
        app:itemIconTint="@color/bottom_nav_color"
        app:itemTextColor="@color/bottom_nav_color"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/bottom_nav_passenger" />
</androidx.constraintlayout.widget.ConstraintLayout>